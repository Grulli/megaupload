  <center><% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div></center>
  <% end %>
<%= form_for(@user) do |f| %>
  <center>
  <div class="field">
    <%= f.text_field :mail, :class=>'input-block-level', :placeholder=>'Email Address'%>
  </div>
  <div class="field">
    <%= f.password_field :password, :class=>'input-block-level', :placeholder=>'Password'%>
  </div>
  <div class="field">
	<%= f.password_field :password_confirmation, :class=>'input-block-level', :placeholder=>'Password Confirmation'%>
  </div>
  <div class="actions">
    <%= f.submit 'Sign Up', :class => 'btn btn-large btn-primary' %></center>
  </div>
<% end %>
