<%= form_for(@up_file) do |f| %>
  <% if @up_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@up_file.errors.count, "error") %> prohibited this up_file from being saved:</h2>

      <ul>
      <% @up_file.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :Url %><br />
    <%= f.text_field :Url %>
  </div>
  <div class="field">
    <%= f.label :mail %><br />
    <%= f.text_field :mail %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
